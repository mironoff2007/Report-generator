 Путь к файлам - корень проекта

1) "4) Пример вызова генератора java Generator.class settings.xml source-data.tsv example-report.txt"
   - С вызовом не понял, как оно должно вызываться так. Не сталкивался с таким. Вызывается в коде через конструктор.

2)  "л) ширина страницы не включает в себя разделитель строк"
   - разделитель строк разделяет строки, а это по высоте. Тут не ясно